# Testing This Project

> **UNCONFIGURED TEMPLATE** — This file contains placeholder guidance only.
> Do not treat any command, coverage target, directory path, or policy below
> as a current project fact. Populate every section with verified information
> from your repository before relying on it. An agent MUST NOT execute or
> assume any instruction here until a human confirms the populated content.

## Test Commands

<!-- Replace each bullet with verified test commands for YOUR repository.
     Only list commands that exist in your build scripts or configuration. -->

- <!-- e.g., `<your-test-runner> <your-test-command>` — runs the full suite -->
- <!-- e.g., `<your-test-runner> <your-unit-test-command>` — unit tests only -->
- <!-- e.g., `<your-test-runner> <your-integration-test-command>` — integration tests only -->

## Test Coverage Requirements

<!-- Replace with verified coverage policies for YOUR repository.
     Do not invent coverage percentages or requirements. -->

- <!-- e.g., Minimum coverage target (if enforced by your tooling) -->
- <!-- e.g., Policy for new-feature test requirements -->
- <!-- e.g., Policy for critical-path integration test coverage -->

## Running Tests

<!-- Replace with verified steps for YOUR repository. -->

1. <!-- e.g., Install dependencies: `<your-install-command>` -->
2. <!-- e.g., Run tests: `<your-test-command>` -->
3. <!-- e.g., Review results: `<your-report-location-or-format>` -->

## Adding New Tests

<!-- Replace with verified conventions for YOUR repository. -->

1. <!-- e.g., Place new test files in `<your-test-directory>` -->
2. <!-- e.g., Follow your project's test naming and structure conventions -->
3. <!-- e.g., Ensure new tests are discovered by `<your-test-runner>` -->
4. Update `ktlo/` if testing standards change.

## How to Populate This File

During repository setup, replace every placeholder comment above with verified
commands, directories, and policies from your repository. Remove this "How to
Populate" section and the UNCONFIGURED TEMPLATE banner only after a human
confirms that every instruction is accurate and executable.
