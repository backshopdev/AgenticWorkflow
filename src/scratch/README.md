# Scratch

This consumer-facing temporary directory is a place for agents to save state
and temporary artifacts between sessions. Contents are project-specific and
are not managed or overwritten by `update` after the directory is created.
